# neovim-config-file

## Prerequisites

``` sh
choco install fd
choco install llvm
choco install ripgrep
```

## Installation

- put Init.vim into your %localappdata%/nvim folder
- run :PlugInstall in NeoVim
